rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // All access goes through Cloud Functions (Admin SDK), so deny all client access
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
